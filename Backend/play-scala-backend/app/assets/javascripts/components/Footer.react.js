var React = require('react');

var Footer = React.createClass({
    getDefaultProps() {
        return {componentClass: 'p'};
    },
    render: function () {
        return (
            <div>

            </div>
        );
    },
});

module.exports = Footer;
